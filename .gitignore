# === Secrets: never commit anything that can sign or authenticate ===
.env
.env.*
!.env.example
*.env
secrets/
*.key
*.pem
*.p12
*.pfx
id_*
id_*.json

# Solana / wallet signing material (never commit)
keypair.json
*-keypair.json
*keypair*.json
wallet.json
*.wallet
phantom-export*
seed.txt
mnemonic.txt
*.mnemonic

# Generic credential-looking files
credentials.json
service-account*.json
*.pkcs8

# === Build output ===
site/dist/
dist/
build/

# === Generated monitoring / report artifacts ===
monitoring/output/
*.report.json
*.report.txt

# === Moderator wallet mapping (personal data: wallet + telegram) kept LOCAL ===
# Only the on-chain lock addresses go into the public transparency record.
launch/moderators.json

# === Local-only launch internals (kept OFF the public repo) ===
# Superseded by scripts/calculate-pumpfun-launch-buy.js; the verification doc
# below records the creator wallet balance, so it stays local.
docs/launch-verification-2026-09.md
scripts/pumpfun-creator-buy.js

# === Volatile launch run outputs (regenerated by scripts; not source) ===
# Simulation examples under launch/generated/simulation/ ARE committed.
launch/generated/member-distribution-ledger.json
launch/generated/MEMBER-DISTRIBUTION-PLAN.md
launch/generated/MEMBER-SNAPSHOT-REPORT.md
launch/generated/LAUNCH-RECONCILIATION.md
launch/generated/MODERATOR-LOCK-PLAN.md

# === Node ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === OS / editor noise ===
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~
